@model Rooms
@{
    ViewData["Title"] = "Delete Room";
}
<div class="modal fade" id="delmodel_@Model.RoomId" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">@ViewData["Title"] </h5>
                <button type="button" class="ms-auto btn-modal-close" data-bs-dismiss="modal" aria-label="Close"><i class='bx bx-plus'></i></button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete this Room?</p>
            </div>
            <div class="modal-footer justify-content-center">
                <form asp-action="Delete" method="post">
                    <input type="hidden" name="id" value="@Model.RoomId" />
                    <button type="submit" class="btn btn-danger">Yes</button>
                    <button type="button" class="btn btn-warning" data-bs-dismiss="modal">Cancel</button>
                </form>
            </div>
        </div>
    </div>
</div>